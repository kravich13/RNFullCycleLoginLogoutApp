export interface IKeychainLoginDataCredentials {
  accessToken: string;
  refreshToken: string;
  id: number;
  username: string;
  email: string;
  firstName: string;
  lastName: string;
  gender: string;
  imageUrl: string;
}
